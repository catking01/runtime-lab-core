# Maintainer Workflow

Validate from a clean public checkout. Review the file manifest, hash manifest, license file, NOTICE attribution status, non-claim registry, public CI result, annotated tag boundary, and release state. GitHub release creation requires a separate explicit release authorization gate.

External contributions are maintainer-reviewed. Accepted contributions are expected to be licensed under Apache-2.0 unless explicitly stated otherwise.
